import { Box, Typography } from "@mui/material";

export default function CustomersTable() {
  // pull customers form the customers provider
  return (
    <Box sx={{ mt: 5 }}>
      <Typography variant="h4">Customers Table</Typography>
    </Box>
  );
}
